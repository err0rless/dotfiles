setw -g mode-keys vi

# Window index starts from 1
set -g base-index 1

# remap prefix from 'C-b' to 'C-a'
set-option -g prefix C-a
unbind-key C-b
bind-key C-a send-prefix

# Key maps
bind -r k select-pane -U 
bind -r j select-pane -D 
bind -r h select-pane -L 
bind -r l select-pane -R

bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

bind x kill-pane # without confirmation
bind S setw synchronize-panes # toggle sync input to all panes
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# TERM
set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

# DELTA: https://dandavison.github.io/delta/tips-and-tricks/using-delta-with-tmux.html
set -ga terminal-overrides ",*-256color:Tc"

# Set the active/inactive window styles
set -g window-style 'fg=colour247,bg=colour236'
set -g window-active-style 'fg=colour250,bg=black'

# Set the pane border style
set -g pane-border-style 'fg=colour238,bg=colour235'
set -g pane-active-border-style 'fg=colour51,bg=colour236'

# Mouse
set -g mouse on

# Enable focus events for nvim integration
set -g focus-events on

# For better life
set -g history-limit 10000
set -s escape-time 0
set -g allow-rename off

#
# Tmux Plugin Manager
#
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'christoomey/vim-tmux-navigator'

# Theme
set -g @plugin "nordtheme/tmux"
set -g @nord_tmux_theme 'dark'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
